[Unit]
Description=public-nginx (docker compose)

[Service]
WorkingDirectory=%h/apps/public-nginx
ExecStart=/usr/bin/docker compose up
ExecStop=/usr/bin/docker compose down
Restart=always
RestartSec=5

[Install]
WantedBy=default.target
